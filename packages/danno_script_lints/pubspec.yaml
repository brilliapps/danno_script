# pubspec.yaml
name: danno_script_lints
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.5.1 <4.0.0"

dependencies:
  dart_eval: ^0.7.9 
  # we will use analyzer for inspecting Dart files
  analyzer: 
  analyzer_plugin:
  # custom_lint_builder will give us tools for writing lints
  custom_lint_builder: ^0.6.5
